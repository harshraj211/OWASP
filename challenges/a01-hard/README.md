# Asterion Benefits Exchange — A01 Hard

Credentialed CORS misconfiguration and cross-origin data exposure lab.
Employee login: `jamie.lee@asterion.local` / `AsterionMember!42`.

Run with `npm install && npm start`, or use the platform launcher. The flag is
generated per process and the intended API target is deliberately hidden.
