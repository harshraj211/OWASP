# MeridianHR — Internal Employee Portal

Hard vertical privilege escalation lab using Node.js and Express. The only
deliberate vulnerabilities are documented in `SOLUTION.md` for authors.

Run with `npm install && npm start`, or build the included Dockerfile. Set
`FLAG` externally if the platform needs to collect a particular flag; the app
generates a fresh UUID flag per process by default.
